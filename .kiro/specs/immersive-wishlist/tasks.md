# Implementation Plan: Immersive Wishlist

## Overview

Add a client-side, guest-friendly wishlist to the immersive store. No new files — all changes are additive edits to five existing files: `assets/immersive-store.js`, `sections/immersive-canvas.liquid`, `sections/glass-product.liquid`, `snippets/immersive-product-card.liquid`, and `locales/en.default.json`.

---

## Tasks

- [x] 1. Add locale keys to `locales/en.default.json`
  - Open `locales/en.default.json` and locate the `"sections"."immersive_store"` object
  - Add a `"wishlist"` object with the following keys:
    - `"button_aria": "Wishlist"`
    - `"panel_title": "Your Wishlist"`
    - `"close": "Close wishlist"`
    - `"empty": "You haven't saved any products yet."`
    - `"save": "Save {{ title }} to wishlist"`
    - `"saved": "Remove {{ title }} from wishlist"`
    - `"save_label": "Save"`
    - `"saved_label": "Saved"`
    - `"view_product": "View product"`
    - `"remove": "Remove from wishlist"`
    - `"item_count": "{{ count }} saved item"`
    - `"item_count_plural": "{{ count }} saved items"`
  - _Requirements: 9.1, 9.2, 9.3_

- [x] 2. Add wishlist button and panel shell to `sections/immersive-canvas.liquid`
  - [x] 2.1 Add the Wishlist_Button to the immersive header
    - In `sections/immersive-canvas.liquid`, locate `.immersive-header__right`
    - Add a `<button>` with `data-wishlist-open`, `class="immersive-header__icon-btn immersive-header__wishlist-btn"`, and `aria-label="{{ 'sections.immersive_store.wishlist.button_aria' | t }}"` immediately before the existing cart button
    - The button must contain an outline heart SVG (`aria-hidden="true"`) and a `<span data-wishlist-badge hidden aria-hidden="true">0</span>` badge
    - _Requirements: 4.1, 4.2, 4.3, 4.6_
  - [x] 2.2 Add the `#immersive-wishlist-panel` dialog shell
    - Inside `<section id="immersive-store-...">`, after `#immersive-onboarding` and before the closing `</section>`, add the panel `<div>` with:
      - `id="immersive-wishlist-panel"`, `class="immersive-wishlist-panel"`, `role="dialog"`, `aria-modal="true"`, `aria-labelledby="immersive-wishlist-title"`, `hidden`, `data-wishlist-panel`
      - JS-consumed string attributes: `data-msg-empty`, `data-msg-view`, `data-msg-remove`, `data-msg-save-label`, `data-msg-saved-label` — all set via `| t | escape`
      - Inner structure: `.immersive-wishlist-panel__inner` > `.immersive-wishlist-panel__header` (containing `<h2 id="immersive-wishlist-title">` and a close `<button data-wishlist-close>`) + `<div class="immersive-wishlist-panel__body" data-wishlist-body></div>`
    - _Requirements: 5.1, 5.3, 5.6, 6.5, 9.2, 9.4_
  - [x] 2.3 Add wishlist CSS to the `{% stylesheet %}` block
    - Append to the existing `{% stylesheet %}` block in `immersive-canvas.liquid`:
      - `.immersive-header__wishlist-btn` — `position: relative`
      - `.immersive-header__wishlist-badge` — absolute positioned gold pill badge, `hidden` via `[hidden]` selector
      - `.immersive-wishlist-panel` — `position: fixed; inset: 0; z-index: 1100; display: flex; justify-content: flex-end`; `[hidden]` sets `display: none`
      - `.immersive-wishlist-panel__inner` — `width: min(420px, 100vw); height: 100%`; glassmorphism background (`rgba(10,15,30,0.92)`, `backdrop-filter: blur(20px)`, gold left border); flex column
      - `.immersive-wishlist-panel__header`, `.immersive-wishlist-panel__heading`, `.immersive-wishlist-panel__close` — header layout, gold heading, close button with `focus-visible` outline
      - `.immersive-wishlist-panel__body` — `flex: 1; overflow-y: auto; padding: 1.5rem; gap: 1rem`
      - `.immersive-wishlist-card` — 3-column grid (80px image, info, actions); gold border; `border-radius: 0.5rem`
      - `.immersive-wishlist-card img`, `.immersive-wishlist-card__title`, `.immersive-wishlist-card__price` — image sizing, gold title, muted price
      - `.immersive-wishlist-card__actions` button styles — view button (gold tint), remove button (slate, red on hover), `focus-visible` outlines
      - `[data-wishlist-empty]` — centred muted text, `padding: 3rem 1rem`
      - `@media (prefers-reduced-motion: reduce)` block suppressing all panel transitions and animations
    - _Requirements: 5.6, 5.7, 6.4, 10.1, 10.4_

- [x] 3. Add wishlist toggle to `snippets/immersive-product-card.liquid`
  - [x] 3.1 Add the toggle button markup
    - In `snippets/immersive-product-card.liquid`, locate the `.immersive-product-link` `<a>` wrapper (the image link)
    - Add a `<button>` with `data-wishlist-toggle`, `data-product-handle="{{ product.handle }}"`, `aria-pressed="false"`, `class="immersive-product-card__wishlist-toggle"` as a sibling immediately after the closing `</a>` tag of `.immersive-product-link`
    - Set `aria-label`, `data-label-save`, and `data-label-saved` all via `| t: title: product.title | escape`
    - Include both SVGs inside the button: outline heart (`class="immersive-product-card__heart immersive-product-card__heart--outline"`) and filled heart (`class="immersive-product-card__heart immersive-product-card__heart--filled"`), both `aria-hidden="true"`
    - _Requirements: 2.1, 2.5, 2.6, 10.3_
  - [x] 3.2 Add wishlist toggle CSS to the `{% stylesheet %}` block
    - Append to the existing `{% stylesheet %}` block in `immersive-product-card.liquid`:
      - `.immersive-product-card__wishlist-toggle` — `position: absolute; top: 0.5rem; right: 0.5rem; width: 32px; height: 32px`; circular dark background; gold border; `z-index: 2`; hover scale and border-color transition
      - `.immersive-product-card__wishlist-toggle:focus-visible` — `outline: 2px solid #d4af37`
      - `.immersive-product-card__heart--filled { display: none }` — hidden by default
      - `.is-saved` modifier rules — hide outline heart, show filled heart
      - `@media (prefers-reduced-motion: reduce)` block suppressing transform and transition
    - _Requirements: 2.6, 10.2, 10.3, 10.4_

- [x] 4. Add wishlist toggle to `sections/glass-product.liquid`
  - [x] 4.1 Add the toggle button markup
    - In `sections/glass-product.liquid`, locate the `{% endform %}` closing tag of the product form inside the `{% if panel_product %}` block
    - Add a `<button>` with `data-wishlist-toggle`, `data-product-handle="{{ panel_product.handle }}"`, `aria-pressed="false"`, `class="glass-product-section__wishlist-toggle"` immediately after `{% endform %}`
    - Set `aria-label`, `data-label-save`, `data-label-saved` via `| t: title: panel_product.title | escape`
    - Include outline and filled heart SVGs (both `aria-hidden="true"`) and a `<span class="glass-product-section__wishlist-label" aria-hidden="true">` showing `{{ 'sections.immersive_store.wishlist.save_label' | t }}`
    - _Requirements: 3.1, 9.2, 10.3_
  - [x] 4.2 Add wishlist toggle CSS to the `{% stylesheet %}` block
    - Append to the existing `{% stylesheet %}` block in `glass-product.liquid`:
      - `.glass-product-section__wishlist-toggle` — full-width pill button (max 300px, centred), transparent background, gold border, `border-radius: 999px`; flex row with gap for icon + label; hover and `.is-saved` state styles
      - `.glass-product-section__wishlist-toggle:focus-visible` — `outline: 2px solid #d4af37`
      - `.glass-product-section__heart--filled { display: none }` — hidden by default
      - `.is-saved` modifier rules — hide outline heart, show filled heart, gold border and background tint
      - `@media (prefers-reduced-motion: reduce)` block suppressing transition
    - _Requirements: 3.1, 10.2, 10.3, 10.4_

- [x] 5. Add WishlistManager state and persistence to `assets/immersive-store.js`
  - [x] 5.1 Add constants and state variables
    - After the existing `var ONBOARDING_KEY = 'immersive_onboarding_seen';` line, add:
      - `var WISHLIST_KEY = 'immersive_wishlist';`
      - `var _wishlistItems = [];`
      - `var _wishlistProductCache = {};`
      - `var _wishlistPanelTrigger = null;`
    - _Requirements: 1.1_
  - [x] 5.2 Implement `getWishlist()`, `_persistWishlist()`, `addToWishlist()`, `removeFromWishlist()`, `toggleWishlistItem()`
    - Add these five functions after `showImmersiveOnboardingIfNeeded` in `immersive-store.js`
    - `getWishlist()` — returns `_wishlistItems.slice()` (shallow copy, no mutation)
    - `_persistWishlist()` — wraps `localStorage.setItem(WISHLIST_KEY, JSON.stringify(_wishlistItems))` in `try/catch`
    - `addToWishlist(handle, source)` — idempotent push; calls `_persistWishlist()`, `updateWishlistBadge()`, `syncAllWishlistToggles(document)`, `trackImmersiveEvent('wishlist_add', { product_handle: handle, source: source })`
    - `removeFromWishlist(handle, source)` — filters `_wishlistItems`; calls `_persistWishlist()`, `updateWishlistBadge()`, `syncAllWishlistToggles(document)`, `trackImmersiveEvent('wishlist_remove', { product_handle: handle, source: source })`
    - `toggleWishlistItem(handle, source)` — delegates to `addToWishlist` or `removeFromWishlist` based on `_wishlistItems.indexOf(handle) !== -1`
    - _Requirements: 1.1, 1.2, 1.3, 1.4, 1.5, 1.6, 1.7, 8.1, 8.2_
  - [x] 5.3 Implement `updateWishlistBadge()`
    - Add `function updateWishlistBadge()` after the functions in 5.2
    - Get `[data-wishlist-badge]`; if not found, return early
    - Set `badge.textContent = _wishlistItems.length`
    - Toggle `badge.hidden` based on `_wishlistItems.length === 0`
    - _Requirements: 4.2, 4.3, 4.4, 7.4_
  - [x] 5.4 Implement `syncAllWishlistToggles(root)`
    - Add `function syncAllWishlistToggles(root)` after `updateWishlistBadge`
    - Query `root.querySelectorAll('[data-wishlist-toggle]')`
    - For each toggle: read `data-product-handle`; compute `isSaved = _wishlistItems.indexOf(handle) !== -1`
    - Set `toggle.setAttribute('aria-pressed', isSaved ? 'true' : 'false')`
    - Toggle `toggle.classList.toggle('is-saved', isSaved)`
    - Update `toggle.setAttribute('aria-label', isSaved ? toggle.getAttribute('data-label-saved') : toggle.getAttribute('data-label-save'))`
    - _Requirements: 2.2, 2.3, 2.4, 3.2, 7.2, 7.3_

- [x] 6. Add WishlistManager panel functions to `assets/immersive-store.js`
  - [x] 6.1 Implement `cacheWishlistProduct(handle, panelEl)`
    - Add `function cacheWishlistProduct(handle, panelEl)` after `syncAllWishlistToggles`
    - Extract from `panelEl`: title from `.glass-product-section__title`, price from `.glass-product-section__price`, image src from `.glass-product-section__media-main img`
    - Store as `_wishlistProductCache[handle] = { title, price, imageSrc }`; skip if any value is missing
    - _Requirements: 5.2_
  - [x] 6.2 Implement `renderWishlistPanel()`
    - Add `function renderWishlistPanel()` after `cacheWishlistProduct`
    - Get `[data-wishlist-body]` and `[data-wishlist-panel]`; return early if not found
    - Read JS string attributes from the panel element: `data-msg-empty`, `data-msg-view`, `data-msg-remove`
    - If `_wishlistItems.length === 0`: set `body.innerHTML = '<p data-wishlist-empty>' + emptyMsg + '</p>'`; return
    - Otherwise build an HTML string: for each handle in `_wishlistItems`, look up `_wishlistProductCache[handle]` for title/price/image; render an `.immersive-wishlist-card` article with image, info, and two action buttons (`data-wishlist-view` and `data-wishlist-remove`, both with `data-product-handle`)
    - Set `body.innerHTML` to the built string
    - _Requirements: 5.2, 5.3, 5.4, 9.4_
  - [x] 6.3 Implement `openWishlistPanel()` and `closeWishlistPanel()`
    - Add both functions after `renderWishlistPanel`
    - `openWishlistPanel()`:
      - Set `_wishlistPanelTrigger = document.activeElement`
      - Call `renderWishlistPanel()`
      - Get `#immersive-wishlist-panel`; call `panel.removeAttribute('hidden')`
      - Call `openDialogFocus(panel, _wishlistPanelTrigger)` — reuse existing helper; the close button selector must match `[data-wishlist-close]`, so pass it as the first focusable target via `panel.querySelector('[data-wishlist-close]')`
      - Fire `trackImmersiveEvent('wishlist_panel_opened', { item_count: _wishlistItems.length })`
    - `closeWishlistPanel()`:
      - Get `#immersive-wishlist-panel`; call `panel.setAttribute('hidden', '')`
      - Call `closeDialogFocus(panel, _wishlistPanelTrigger)`
      - Set `_wishlistPanelTrigger = null`
    - _Requirements: 5.1, 5.6, 6.1, 6.2, 6.3, 6.4, 8.4_

- [x] 7. Add `initWishlist()` and wire all event handling in `assets/immersive-store.js`
  - [x] 7.1 Implement `initWishlist()`
    - Add `function initWishlist()` after `closeWishlistPanel`
    - Load from `localStorage`: wrap `JSON.parse(localStorage.getItem(WISHLIST_KEY) || '[]')` in `try/catch`; validate result is an array; fall back to `[]`
    - Call `updateWishlistBadge()` and `syncAllWishlistToggles(document)`
    - _Requirements: 1.1, 1.2, 1.3, 7.1, 7.2_
  - [x] 7.2 Add the delegated click listener for all wishlist interactions
    - Add a single `document.addEventListener('click', function(e) { ... })` call inside `initWishlist()`, after the badge/toggle sync
    - Route clicks using `e.target.closest(...)`:
      - `[data-wishlist-open]` → `openWishlistPanel()`
      - `[data-wishlist-close]` → `closeWishlistPanel()`
      - `[data-wishlist-toggle]` → read `data-product-handle`; determine `source` (`'product_panel'` if inside `#glass-panel`, else `'product_card'`); call `toggleWishlistItem(handle, source)`
      - `[data-wishlist-remove]` → read `data-product-handle`; call `removeFromWishlist(handle, 'wishlist_panel')`; remove the closest `[data-wishlist-card]` from the DOM; if `[data-wishlist-body]` is now empty, call `renderWishlistPanel()` to show empty state
      - `[data-wishlist-view]` → read `data-product-handle`; fire `trackImmersiveEvent('wishlist_view_product', { product_handle: handle })`; call `closeWishlistPanel()`; call `openProductPanel(handle, null)`
    - Add Escape key handling: `document.addEventListener('keydown', function(e) { if (e.key === 'Escape' && !document.getElementById('immersive-wishlist-panel').hidden) closeWishlistPanel(); })`
    - _Requirements: 4.5, 5.4, 5.5, 6.3, 8.3, 8.4, 8.5_
  - [x] 7.3 Hook `cacheWishlistProduct` and `syncAllWishlistToggles` into `openProductPanel`
    - In `assets/immersive-store.js`, locate the `render()` function inside `openProductPanel` — specifically the block after `loadProductRecommendations(panel)`
    - Add two calls as the last lines inside `render()`:
      - `cacheWishlistProduct(productHandle, panel);`
      - `syncAllWishlistToggles(panel);`
    - _Requirements: 3.2, 7.3_
  - [x] 7.4 Hook `syncAllWishlistToggles` into `openCollectionPanel`
    - In `assets/immersive-store.js`, locate the `render()` function inside `openCollectionPanel` — the block that sets `contentArea.innerHTML` and calls setup functions
    - Add `syncAllWishlistToggles(panel);` as the last call inside that render block
    - _Requirements: 7.3_
  - [x] 7.5 Call `initWishlist()` from `bindImmersiveInit()`
    - In `assets/immersive-store.js`, locate `bindImmersiveInit()` and the `requestAnimationFrame` callback
    - Add `initWishlist();` as the last call inside the `requestAnimationFrame` callback, after `showImmersiveOnboardingIfNeeded()`
    - _Requirements: 7.1, 7.2_

- [x] 8. Final verification
  - Confirm `locales/en.default.json` contains all 12 wishlist keys under `sections.immersive_store.wishlist`
  - Confirm `[data-wishlist-toggle]` buttons exist in both `immersive-product-card.liquid` and `glass-product.liquid` with correct `data-product-handle`, `aria-pressed`, `data-label-save`, `data-label-saved` attributes
  - Confirm `#immersive-wishlist-panel` has `role="dialog"`, `aria-modal="true"`, `aria-labelledby="immersive-wishlist-title"`, and all five `data-msg-*` attributes
  - Confirm `[data-wishlist-badge]` is present in the header button and starts `hidden`
  - Confirm `initWishlist()` is called from `bindImmersiveInit()` and `syncAllWishlistToggles(panel)` is called from both `openProductPanel` and `openCollectionPanel` render functions
  - _Requirements: 1–10 (all)_
